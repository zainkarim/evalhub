# Leave unset to run the UI on sample data from src/lib/mockData.js.
# Point this at the Express server once Sprint 1 endpoints are up.
VITE_API_URL=http://localhost:4000/api

# Force sample data even when VITE_API_URL is set.
VITE_USE_MOCKS=false
